# Copy these values into a local, untracked env file or export them in your shell.
# Set PHI_ENV_FILE="" to disable dotenv loading entirely (the test suite does).

# Server mode. Anything other than local/test/demo is treated as production.
PHI_ENVIRONMENT=local
